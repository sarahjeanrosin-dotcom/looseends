{
  "name": "release-impact-finder",
  "private": true,
  "version": "0.0.0",
  "description": "Release Impact Finder — audits website + SharePoint content for relevance to an upcoming product release.",
  "scripts": {
    "dev:client": "npm run dev --workspace client",
    "build:client": "npm run build --workspace client",
    "dev": "netlify dev",
    "crawl:test": "tsx scripts/crawl-test.ts",
    "audit:test": "tsx scripts/run-audit-test.ts",
    "sharepoint:crawl": "tsx scripts/sharepoint-crawl.ts"
  },
  "workspaces": [
    "client"
  ],
  "devDependencies": {
    "@netlify/functions": "^3.0.0",
    "@types/jsdom": "^30.0.0",
    "dotenv": "^17.4.2",
    "tsx": "^4.23.13",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.125.0",
    "@mozilla/readability": "^0.6.0",
    "@supabase/supabase-js": "^2.45.4",
    "fast-xml-parser": "^5.11.1",
    "jsdom": "^25.0.1",
    "zod": "^4.6.5"
  }
}

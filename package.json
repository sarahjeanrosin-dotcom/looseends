{
  "name": "release-impact-finder",
  "private": true,
  "version": "0.0.0",
  "description": "Release Impact Finder — audits website + SharePoint content for relevance to an upcoming product release.",
  "scripts": {
    "dev:client": "npm run dev --workspace client",
    "build:client": "npm run build --workspace client",
    "dev": "netlify dev"
  },
  "workspaces": [
    "client"
  ],
  "devDependencies": {
    "@netlify/functions": "^3.0.0",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4"
  }
}
